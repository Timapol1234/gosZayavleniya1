# Инструкция по установке и запуску проекта

## Предварительные требования

Перед началом убедитесь, что у вас установлено:

- **Node.js** 20+ LTS ([скачать](https://nodejs.org/))
- **npm** или **yarn** (устанавливается вместе с Node.js)

**Примечание:** Проект использует SQLite, поэтому отдельная установка базы данных не требуется!

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта `D:\gosZayavleniya` и выполните:

```bash
npm install
```

Эта команда установит все необходимые зависимости из `package.json`.

## Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и настройте переменные окружения:

```env
# Database (SQLite) - уже настроено
DATABASE_URL="file:./prisma/dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate_random_secret_here"

# Остальные переменные оставьте пустыми для начала
```

### Генерация NEXTAUTH_SECRET

Выполните в терминале:

```bash
openssl rand -base64 32
```

Скопируйте полученную строку и вставьте в `.env` как значение `NEXTAUTH_SECRET`.

## Шаг 3: Инициализация базы данных

### 3.1 Создайте миграции и таблицы

```bash
npx prisma migrate dev --name init
```

Эта команда:
- Создаст файл базы данных SQLite (`prisma/dev.db`)
- Создаст таблицы согласно схеме `prisma/schema.prisma`
- Автоматически выполнит seed скрипт, который добавит:
  - 6 категорий заявлений
  - 2 шаблона (Налоговый вычет, Претензия в банк)
  - Тестового пользователя (email: `test@example.com`, пароль: `password123`)

### 3.2 Проверьте базу данных (опционально)

Откройте Prisma Studio для визуального просмотра данных:

```bash
npm run db:studio
```

Откроется браузер с интерфейсом для просмотра таблиц и данных.

## Шаг 4: Запуск проекта

### Режим разработки

```bash
npm run dev
```

Проект будет доступен по адресу: **http://localhost:3000**

### Production сборка (для тестирования)

```bash
npm run build
npm run start
```

## Шаг 5: Проверка работы

Откройте браузер и перейдите на:

- **Главная страница:** http://localhost:3000
- **Шаблоны:** http://localhost:3000/templates (страница еще не создана, вернет 404)
- **Личный кабинет:** http://localhost:3000/dashboard (требует авторизации)

## Возможные проблемы и решения

### Ошибка миграции Prisma

**Проблема:** `Error: P3006: Migration ... failed to apply`

**Решение:**
```bash
npx prisma migrate reset  # Это сбросит БД и пересоздаст таблицы
```

### Порт 3000 уже занят

**Решение:**
Измените порт в команде запуска:
```bash
PORT=3001 npm run dev
```

## Полезные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск в режиме разработки |
| `npm run build` | Сборка production версии |
| `npm run start` | Запуск production сборки |
| `npm run lint` | Проверка кода ESLint |
| `npm run format` | Форматирование кода Prettier |
| `npm run type-check` | Проверка типов TypeScript |
| `npm run db:migrate` | Создание новой миграции |
| `npm run db:seed` | Заполнение базы тестовыми данными |
| `npm run db:studio` | Открыть Prisma Studio |

## Следующие шаги

После успешного запуска можно приступить к разработке следующих модулей:

1. **Страница шаблонов** (`/templates`)
2. **Страница авторизации** (`/login`, `/register`)
3. **Создание заявления** (`/create/[templateId]`)
4. **Личный кабинет** (`/dashboard`)

Следуйте плану разработки из `development-plan.md`.

## Дополнительная информация

- [Документация Next.js](https://nextjs.org/docs)
- [Документация Prisma](https://www.prisma.io/docs)
- [Документация TailwindCSS](https://tailwindcss.com/docs)

---

**Успехов в разработке!** 🚀
